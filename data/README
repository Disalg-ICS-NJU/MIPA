* temperature sensor data source format:

first line: time gap.
temperature sensor data per line by following.



* RFID data source format:

first line: time gap.
sencond line: the number of tags.
tags id per line by following.
tags data per line by following as the order of tags id.



* light sensor data source format:

first line: time gap.
light sensor data per line by following.


* distance sensor data source format:

first line: time gap.
distance sensor data per line by following.
distance value with timestamps.